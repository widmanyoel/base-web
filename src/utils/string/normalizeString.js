/**
 * converts `string` into loweCase string
 *
 * @private
 * @param {String} string.
 * @returns {String} Returns the normalized string
 */

export const normalizeString = (string = '') => string.toLowerCase()
